namespace Crownsfall.Combat
{
    public static class CombatDebug
    {
        public static bool TracePresentation = true;

        /// <summary>
        /// Logs local PlayerPrefs progress (run records, Character Builder display).
        /// </summary>
        public static bool LocalProgressDebug = true;
    }
}
